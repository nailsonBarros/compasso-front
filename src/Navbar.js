import React from 'react';

const Navbar = () => (
    <nav className="navbar navbar-dark bg-info mb-3">
        <div className="container">
            <a href="#" className="navbar-brand">Compasso Front</a>
        </div>
    </nav>
);

export default Navbar;